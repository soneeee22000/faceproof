# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Environment
.env
.env.local

# Model weights — downloaded via scripts, never committed
models/*.pth
models/*.pt
models/*.onnx
models/weights/

# ...except FaceProof's own trained anti-spoofing model (committed deliberately)
!models/antispoofing_mobilenetv2.pth

# Datasets — never committed (licensing: see data/README.md)
data/lfw/
data/celeba_spoof/
data/**/*.jpg
data/**/*.png
data/**/*.zip

# Local agent / editor config — not part of the published project
CLAUDE.md

# Evaluation — transient scoring checkpoint (final results are committed, not this)
evaluation/results/lfw_checkpoint.npz

# Frontend
frontend/node_modules/
frontend/dist/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
